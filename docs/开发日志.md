# 开发日志

## 重要备忘

- 版本信息修改：`web\static\config.json`
- 开发测试用户：tabor，密码：123456
- 构建和运行：

```bash
go build -o taskgo.exe ./cmd/main.go # Build
.\taskgo.exe # Run the Go application.
```

## Todo

## 20251116
- API Tokens 需要可以设置名称，以区别不同token对应的使用对象。


- 用户增加 id 属性，用户上传任务图片需上传到 web\static\uploads\tasks\user_id 文件夹中。
- 右上角搜索框输入内容后，右侧出现一个清除输入的图标，点击可以删除输入到内容。
- 点击任务卡片菜单栏，Delete Task 按钮，需弹出自定义删除对话框，全英文页面。
- 移动端，侧边栏点击标签筛选后，自动关闭侧边栏。
- 右上角搜索框右侧的图标，需要上下居中显示，目前移动端时右上显示，需要居中。
- 手机浏览器点击侧边栏 Settings，会跳出 PWA 到浏览器，请修复。
- 任务卡片菜单栏，点击“Add Tag”没有反应，需要实现可以对现有任务增加新的标签。
- 设置页面增加 Token 生成和管理，点击可以生成 token,token 有效期默认 1 个月，可以设置其他时间（1 天、1 月，1 年、永久），点击可以复制 token 到剪贴板,token 删除或更新后之前的 token 失效。
- 苹果手机，如何使用快捷指令（快捷指令如何制作、如何设置 token），实现快速添加任务，用户复制内容，双击手机背面，会弹出选择任务标签（基于 token 获取），用户选择后自动把复制内容发送到 TaskGo 对应的标签中。
- 侧边栏下方任务总数图标靠右一点。
- 移动端时，编辑任务卡片时，该任务上滑，任务卡片底部需要在输入法弹窗的上方。
- 手机浏览器时，点击右上角搜索框时，页面不要放大。
- Tasks 边框距离浏览器边缘再近一点，任务卡片距离外侧再近一点。
- 移动端，返回到最顶部按钮图标，需要在下方输入框的顶部显示，返回到顶部图标需要可以拖动，并记住用户最后一次拖动放置的位置。
- 上传图片需转换为 webp 格式，如无法转换则保留原格式。

## 20251115

- 实现动态主题色跟随页面内容变化，通过计算页面元素背景色动态更新 PWA 状态栏颜色。
- 鼠标指到用户信息区头像下显示总任务数与已完成数，给出提示，指到总任务数显示“总任务数 X 个”，指到已完成数显示“已完成任务 X 个”
